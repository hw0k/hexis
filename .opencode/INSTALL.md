# Install hexis for OpenCode

Add to your `opencode.json`:

```json
{
  "plugin": ["hexis@git+https://github.com/hw0k/hexis.git"]
}
```

Restart OpenCode. All hexis skills are registered automatically.

To verify: ask "what hexis skills are available?" or run `hexis:dispatch`.
